main
  │
  ├── create branch
  ▼
features
  │
  ├── code changes
  ├── git add .
  ├── git commit
  └── git push origin features
          │
          ▼
      Pull Request
          │
          ▼
       Merge
          │
          ▼
         main

git checkout -b features

# make changes

git add .
git commit -m "Added endsemtesting"

git push origin features

git checkout -b features

# make changes

git add .
git commit -m "Added endsemtesting"

git push origin features
git checkout -b features

# make changes

git add .
git commit -m "Added endsemtesting"

git push origin features